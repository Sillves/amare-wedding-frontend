# API Configuration
#
# Kopieer dit bestand naar .env.local voor lokale development
# .env.local wordt NIET gecommit naar git
#
# Voor lokale development:
VITE_API_URL=http://localhost:5072/api
#
# Backend URL for fetching OpenAPI spec (optional, defaults to VITE_API_URL without /api)
# BACKEND_URL=http://localhost:5072
#
# OpenAPI endpoint path (optional, defaults to /swagger/v1/swagger.json)
# OPENAPI_ENDPOINT=/swagger/v1/swagger.json
#
# Voor production wordt .env.production gebruikt (al geconfigureerd)
